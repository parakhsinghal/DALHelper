name: Build Sample NuGet Package Project

on:
  push:
    tags: v*

env:
  PROJECT_LOCATION: DALHelper/DALHelper/DALHelper
  PACKAGE_OUTPUT_DIRECTORY: ${{ github.workspace }}
  NUGET_SOURCE_URL: 'https://api.nuget.org/v3/index.json'
  RELEASE_VERSION: ${GITHUB_REF#refs/tags/v}

jobs:
  build:
    runs-on: ubuntu-latest    

    steps:
    - name: Checkout
      uses: actions/checkout@v2
    
    - name: Setup .NET
      uses: actions/setup-dotnet@v1
      with:
        dotnet-version: 5.0.x
        
    - name: Restore dependencies
      run: dotnet restore {{ env.PROJECT_LOCATION }}
      
    - name: Build
      run: dotnet build {{ env.PROJECT_LOCATION }} --no-restore --configuration Release
      
    - name: Test
      run: dotnet test {{ env.PROJECT_LOCATION }} --no-build --verbosity normal
    
    - name: Pack Project
      run: dotnet pack {{ env.PROJECT_LOCATION }} --no-restore --no-build --configuration Release --include-symbols -p:PackageVersion=${{ env.RELEASE_VERSION }} --output ${{ env.PACKAGE_OUTPUT_DIRECTORY }}
      
    - name: Push Package
      run: dotnet nuget push ${{ env.PACKAGE_OUTPUT_DIRECTORY }}/*.nupkg --api-key ${{ secrets.NUGET_KEY }} --source ${{ env.NUGET_SOURCE_URL }} --skip-duplicate
